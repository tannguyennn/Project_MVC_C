@model IEnumerable<Project_63132204.Models.LoaiDichVu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutKH.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLDV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLDV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLDV }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLDV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLDV })
        </td>
    </tr>
}

</table>
